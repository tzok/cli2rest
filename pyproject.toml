[project]
name = "cli2rest"
version = "1.0.1"
description = "Wrap arbitrary CLI tools as a FastAPI service"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
]

[dependency-groups]
dev = [
    "requests",
]
